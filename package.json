---

## 🧪 `package.json`（最小構成）

```json
{
  "name": "lre-backend",
  "version": "0.1.0",
  "description": "Backend API for the LRE memory and ritual system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "fs-extra": "^11.2.0"
  }
}
